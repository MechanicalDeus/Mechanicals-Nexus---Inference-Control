# Editor / agent local (not part of the published tree)
.cursor/

# Bytecode / environment
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.env
.env.*

# Tests / tools
.pytest_tmp/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# Build
dist/
build/
*.egg-info/
*.egg

# --- Nexus: sensitive inference data (do not commit) ---
# Graph exports and similar artefacts mirror structure + paths of scanned code.
*.nexus.json
*.inference.json
*.inference-map.json
*.graph.json

# Legacy / common naming patterns
*-inference.json
*-inferenz.json
inferenz.json
nexus-inference*.json
nexus_export.json

# Internal analysis outputs
nexus_output/
.cache/nexus/
.nexus-cache/
.tmp_nexus_cache/
.tmp/

# Optional: full export directories
exports/

# Local briefing / query dumps (often contain absolute paths)
nexus-package-brief.txt
*-package-brief.txt

# Local one-off analyses
ttrpg-studio-nexus-analyse.md
